import React from 'react';

type RoomModalProps = {
  isOpenModal: boolean;
  toogle: () => void;
};
const RoomModal = ({ isOpenModal, toogle }: RoomModalProps) => {
  return <div></div>;
};

export default RoomModal;
