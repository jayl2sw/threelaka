import React from 'react';
import { useAppDispatch } from '../../../../utils/hooks';

const PickedMode = () => {
  const dispatch = useAppDispatch;

  return <div></div>;
};

export default PickedMode;
